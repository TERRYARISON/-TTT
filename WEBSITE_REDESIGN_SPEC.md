# 郑超个人作品集网站｜最终改版开发任务书

> 给编码 AI / 前端工程师：不要把这份文档当成“灵感参考”，而要把它当成最终产品目标与验收标准。你可以自行选择最稳妥的代码实现方式，但最终效果、信息结构、交互逻辑、性能与响应式行为必须满足本文。

## 0. 任务目标

将郑超（Terry Arison / 郑超）的现有个人作品集网站整体升级为一套具有 **Organic Futurism / Bio-Tech Editorial（有机未来主义 + 生物科技杂志感）** 的高端沉浸式作品集网站。

核心审美关键词：

**纯黑留白 / 半透明仿生生命 / 樱花与植物共生 / 金色神经网络 / 冰蓝生物荧光 / 珍珠玻璃材质 / 毛玻璃 UI / 高对比 Serif 字体 / 缓慢电影镜头 / 漂浮花瓣 / 克制紫色强调 / 轻度赛博朋克。**

目标不是做成“科幻模板网站”，也不是照抄 Bloom 品牌页面。最终必须仍然明显是 **郑超本人及其工作、履历、作品、书、App、收藏与工作室** 的个人网站，只借用参考视觉语言和动画质感。

优先级从高到低：
1. 视觉完成度与第一屏冲击力；
2. 内容可读性和个人身份表达；
3. 交互流畅、动画有叙事意义；
4. 桌面与移动端完整可用；
5. 性能、可访问性、维护性。

---

## 1. 当前网站基线（必须理解并保留）

现有项目是 React + TypeScript + Vite 的单页前端项目，已有 Tailwind CSS、React Router、GSAP、Framer Motion、Lenis 等动画与滚动能力。

现有信息架构：

- `/` 首页：Hero / About Manifesto / 四象限入口 / Selected Work
- `/studio` FreeFrame Studio 工作室
- `/career` 履历
- `/works` 作品入口
- `/books` 长篇书籍
- `/app` App / 数字产品概念
- `/about` 关于我
- `/contact` 联系
- `/work/:slug` 项目详情

现有核心交互：

- 首次访问首页显示 Preloader；同一 session 不重复播放。
- 全站固定导航；向下滚隐藏、向上滚出现；当前路由有状态提示。
- 桌面端自定义光标；可点击对象有 hover 状态。
- 全站 Lenis 平滑滚动。
- 页面元素滚动进入时有 reveal / fade-up。
- Hero 当前有鼠标视差。
- 全站已有花瓣与光尘 canvas 氛围层。
- Work Detail 支持图片 Lightbox。
- Contact 的微信/Line 可一键复制，并显示“已复制”反馈。
- About 的横向身份文字支持 marquee，hover 暂停。
- 已有 `prefers-reduced-motion` 降级逻辑。

**这些功能不要因为改视觉而丢掉。** 可以重构表现方式，但用户能力必须保留或优化。

---

## 2. 全站视觉系统

### 2.1 总体气质

最终感觉应像：

“一个人的私人数字展厅 + 高端时装编辑页 + 生物科技艺术实验室”。

整体必须安静、奢华、神秘、克制，不要变成：

- 传统蓝紫霓虹赛博朋克；
- 游戏 HUD；
- 满屏发光描边；
- 大量卡片堆叠；
- 廉价玻璃拟态；
- 粒子雨；
- 科技公司 SaaS 模板；
- 过量渐变和巨大阴影。

### 2.2 色彩

基础：
- 页面底色：接近纯黑 `#050507 ~ #07070B`
- 主文字：珍珠白 / 暖白 `#F3F0EA`
- 次文字：灰紫 / 冷灰
- 樱花粉：低饱和粉
- 紫色：高饱和但只作为关键交互强调
- 冰蓝 / 青色：只用于“生物荧光”细节
- 香槟金 / 铜金：只用于“神经网络 / 金属丝 / 高级细节”

紫色绝不能大面积铺底。它应该像珠宝一样只出现在：CTA、关键词胶囊、选中态、少量微光。

### 2.3 材质

主要材质：
- true black canvas
- 半透明烟熏玻璃
- 珍珠 / 湿润树脂 / 透明皮肤
- 金属丝 / 毛细血管 / 植物根系混合纹理
- 低透明度毛玻璃
- 极细线条

阴影很少使用。空间关系主要通过：景深、透明度、模糊、亮暗、遮挡、前后景建立。

### 2.4 字体

继续使用“高级 Serif + 极简 Sans”的双字体体系：

- 大标题 / 宣言 / 项目标题：高对比度 Serif，具有杂志气质。
- 导航 / 标签 / 小正文 / 数字：极简 Sans。
- 中文必须与英文在重量和气质上匹配，不允许默认宋体/黑体破坏整体感。

### 2.5 版式

桌面端不要让内容贴满浏览器边缘。

第一屏核心视觉采用：
- 外层纯黑大留白；
- 中央一个大型横向圆角“视觉舞台 / living frame”；
- 主视觉窗口大约占视口宽度 88%–94%，高度 78%–88%；
- 导航悬浮于顶部；
- UI 文案与视觉画面叠加但必须有层次。

全站后续页面继续维持开放式排版，不要改成一页页卡片后台。

---

## 3. 首页：核心改造（最高优先级）

首页第一屏必须成为整站最强记忆点。

### 3.1 首次加载 / Preloader

保留现有“只在 session 首次播放”的逻辑。

视觉改成更克制：
- 全黑；
- 中央现有花形 Logo / 花瓣标志；
- 花瓣轮廓从无到有；
- 极小百分比或细线进度；
- 总时长建议控制在约 1.8–2.6 秒；
- 结束不是生硬消失，而是像幕布 / iris / clip-path 一样揭开主视觉。

如果资源已缓存，应快速完成，不能每次强制等待固定时长。

### 3.2 Hero 主视觉内容

主视觉不是普通“左文字右图片”。

需要制作一张或一段新的核心视觉资产：

**郑超本人作为主体，与樱花、植物、有机线路、半透明玻璃/珍珠材质、生物科技结构发生“共生”。**

可以保留真实人物辨识度，但视觉处理要进入有机未来主义：
- 身体或局部半透明；
- 暖金与冰蓝细线像神经 / 根系 / 电路一样在内部生长；
- 樱花或花卉不是贴图装饰，而像从人物/空间结构中自然生长；
- 有少量微光粒子和花瓣；
- 背景保持暗，景深明显；
- 不要把人物变成银色机器人；
- 不要把脸做成恐怖解剖图；
- 保留优雅、人性、真实感。

如果使用静态图，则用多层前景 / 背景 / mask / parallax 形成电影感。如果使用视频，则必须静音自动播放、循环无明显接缝，并提供静态 poster 和低性能降级。

### 3.3 Hero 第一状态：身份

首次进入 Hero 时看到：

- 左上：品牌标识“郑超 / Zheng Chao”或现有花形 Logo + 名字；
- 顶部右侧：工作室 / 履历 / 作品 / 关于 / 联系；
- 主体：人物 + 有机科技樱花视觉；
- 左下或画面较暗区域：一块低透明度毛玻璃信息面板；
- 主标题沿用现有核心表达：

`All of this is me.`
`None of it is all of me.`

其中 `all of me` 或中文关键表达允许用一个紫色圆角胶囊强调，但整屏最多一个这样的紫色大强调。

辅助信息继续表达现有身份：

`Build · Write · Produce · Antiques & Thai Amulets Collect`

以及现有中文表达“造物、旧物与其他执念”。

主 CTA：进入工作室 / 查看作品；次 CTA：关于我。

CTA 样式：紫色圆形箭头按钮 + 极简文字，不要做普通矩形大按钮。

### 3.4 Hero 滚动叙事

Hero 建议做成一个连续的“滚动驱动电影镜头”，而不是几个独立 section 硬切。

滚动过程至少有 3 个状态：

#### Scene A — Identity
- 人物完整可见；
- 标题、身份、CTA 出现；
- 画面缓慢推近；
- 鼠标移动只产生非常轻的景深偏移，不要追着鼠标乱跑。

#### Scene B — Disciplines
继续向下滚时：
- 原主标题与 CTA 平滑淡出；
- 视觉构图缓慢向人物局部 / 手 / 花 / 神经网络细节移动；
- 出现三张窄的半透明玻璃卡，不占满画面；
- 三张卡分别代表：
  1. BUILD / 造物
  2. WRITE / 写作
  3. PRODUCE / 制作
- 卡片顶部只使用极简小型符号，不使用大图标；
- 卡片出现方式：轻微位移 + 淡入；离场：淡出；无弹跳。

这三张卡点击后可以分别进入最相关内容：App / Books / Studio 或 Works 下的对应内容。

#### Scene C — Manifesto
继续滚动：
- 三卡淡出；
- 画面进一步变得简洁；
- 出现一条跨画面的高级 Serif 宣言；
- 用一个紫色胶囊突出关键词；
- 花瓣与微粒数量略增加，但仍克制；
- 最后自然过渡到首页第二幕内容，而不是突然“结束动画”。

Hero 的滚动动画必须是顺滑连续时间轴，不是元素各自随机触发。

### 3.5 Hero 的桌面/移动区别

桌面：
- 可以使用 sticky/pinned cinematic sequence；
- 有轻鼠标视差；
- 有自定义光标；
- 视觉镜头变化更完整。

移动：
- 不强行复刻复杂 pinned 时间轴；
- 保留核心三状态，但通过正常纵向滚动 + 简化淡入/位移完成；
- 禁用自定义光标；
- 粒子数量至少减半；
- 主视觉裁切必须始终优先保证人物脸部和文字可读；
- CTA 触控面积 >= 44px；
- 不允许横向溢出或文字被裁切。

---

## 4. 首页后续内容

首页后续仍保持现有四幕信息结构，但视觉统一到新系统。

### 4.1 Manifesto / About Me

保留现有双语自述，不要改写用户原文。

表现：
- 大量黑色留白；
- 英文 Serif 大字逐行 reveal；
- 中文正文更轻、更克制；
- 只挑一个关键词做粉 / 紫强调；
- 不再使用多张玻璃卡。

### 4.2 Index / 四条线

仍为：Studio / Career / Works / About。

不要继续使用四张“大玻璃卡网格”的通用模板感。

改成更像展览目录：
- 四行或两列开放式索引；
- 大编号 01–04；
- 标题 + 中文 + 一句描述；
- hover 时才出现极轻的玻璃带 / 花瓣扰动 / 线条移动；
- 进入链接时箭头位移。

### 4.3 Selected Work

保留列表式而不是卡片墙。

桌面 hover 时：
- 当前项目标题变亮/变粉；
- 可在鼠标附近或列表一侧浮出项目缩略图预览；
- 缩略图带轻微惯性跟随，但不能遮住文字；
- 离开行后预览淡出。

移动端：
- 每行直接显示小缩略图或完全不显示悬浮预览；
- 不依赖 hover 才能获取信息。

---

## 5. 全站导航逻辑

保留全站固定导航。

桌面：
- 顶部左侧：花形 Logo + 郑超 / Zheng Chao；
- 右侧导航保持极简；
- 初始 Hero 上导航应接近“裸文字 / 极轻玻璃”；
- 滚出 Hero 后才增强毛玻璃背景；
- 向下连续滚动隐藏，向上滚动立即出现；
- 当前页使用一个极细粉/紫点或细线，不使用粗底色选中块。

移动：
- 左侧 Logo/名字 + 右侧汉堡；
- 点击后出现全屏暗玻璃菜单；
- 菜单项目大字、足够间距；
- 打开菜单锁定背景滚动；
- 进入新路由自动关闭；
- Esc / 返回行为合理。

---

## 6. 自定义光标与微交互

仅桌面精确指针设备启用。

保留现有“内点 + 外环 + 延迟跟随”的概念，但视觉更克制：
- 内点更小；
- 外环细线；
- mix-blend 可保留，但必须确保在浅色图片上仍清晰；
- hover 链接时外环轻微放大；
- 只有项目图片/画廊才允许显示“查看”文字；
- 不要在所有按钮上堆文字提示。

交互元素统一规则：
- hover 位移 2–8px 范围；
- 不使用夸张 scale；
- 不使用弹性 bounce；
- 主要 easing 是 smooth ease-out / ease-in-out；
- 所有交互动效让人感觉“有惯性、柔软、慢半拍”。

---

## 7. 花瓣、粒子、线条氛围层

保留现有 PetalField 概念，但重新分级。

### Hero
- 花瓣数量中等；
- 前景/中景/背景三种尺寸与 blur；
- 花瓣不只是纯椭圆 glow，要更像真实单瓣剪影；
- 少量白色/冰蓝微尘上浮；
- 花瓣轨迹不规则，有横摆与轻微旋转；
- 可让鼠标产生极轻的空间偏移，但不要排斥爆炸。

### 内页
- 数量显著降低；
- 只作为环境颗粒；
- 阅读长文时不能一直有明显花瓣扫过文字。

页面不可见时暂停动画；低电量 / reduced-motion / 移动端需降级。

---

## 8. 内页统一改造规则

内页不要全部重新发明版式。统一使用同一视觉语言，但每页保留自己的内容性格。

### Studio
- 更像工作室能力与案例的 editorial portfolio；
- 服务内容以开放列表、图片带、大片段排版呈现；
- 避免服务卡片墙；
- 场地 / KOL / 制作等图片图库可以使用轻横向 rail 或 masonry；
- 图片 hover 提亮，点击可放大时给明确反馈。

### Career
- 继续使用时间线；
- 时间、角色、项目形成强排版对比；
- 不要用简历模板式卡片；
- 代表项目可通过缩略图和大标题连接到详情。

### Works
- 作为“写作 + 造物”的分流入口；
- 两大内容面应该有明显不同的图像气质，但仍属于同一系统；
- 不使用普通两张大卡片模板感。

### Books
- 书封面必须成为主角；
- 第一本文学作品可以使用更大的 editorial feature；
- 其余书籍形成节奏不同的陈列；
- hover 只做轻抬、角度、亮度变化；
- 不能像电商商品列表。

### App
- 继续展示真实 App 概念 / 产品思考；
- UI 截图像展品一样呈现；
- 不要虚构下载量、用户数、融资等不存在的数据。

### About
- 保留人物照片、双语自述、身份 marquee、收藏与正在做的事；
- 照片统一暗化色彩与 film grain；
- hover 提亮；
- marquee hover 暂停功能保留；
- 不让页面变成“技能标签墙”。

### Contact
- 保留 Email / WeChat-Line / Location 三行大链接结构；
- 微信复制必须工作，并提供 2 秒左右“已复制”反馈；
- 保留工作室合作说明；
- 页面越简单越好。

### Work Detail
- 保留项目 Hero、Overview、Gallery、Quote、Next Project、Lightbox；
- Hero 图有轻微滚动视差；
- Gallery 点击图片打开 Lightbox；
- Lightbox 支持关闭按钮、点击遮罩、Esc；
- 下一项目切换必须明显但克制。

---

## 9. 页面切换

路由切换时：
- 仍回到新页面顶部；
- 不出现白屏；
- 可以增加一个 200–450ms 的黑色淡入淡出 / mask 过渡；
- 不要做长 loading；
- 页面标题、核心图像先出现，非首屏图片 lazy load。

如果浏览器前进 / 后退，应保持行为自然，不出现重复 preloader。

---

## 10. 响应式标准

至少验收：
- 1440px 桌面
- 1024px 小型桌面 / 横屏平板
- 768px 平板
- 430px iPhone Pro Max 级别
- 375px 小屏手机

要求：
- 所有标题都不能被截断；
- 中文/英文混排不能产生奇怪孤字；
- 视觉主体裁切要有人脸保护；
- 触控区不小于 44px；
- 不依赖 hover；
- 横向 gallery 若允许滑动必须有自然触摸惯性；
- `100dvh` 考虑 iOS Safari 动态工具栏；
- safe-area 合理处理。

---

## 11. 性能标准

目标：现代桌面与 iPhone 上保持顺滑，核心动画以 60fps 为目标。

必须做到：
- 首屏资源有明确优先级；
- 非首屏图片 lazy load；
- 图片使用合理尺寸与现代格式；
- 视频提供 poster；
- 不同时运行多个昂贵 blur / filter / canvas 层；
- canvas DPR 上限 2；
- 页面隐藏时暂停 requestAnimationFrame；
- 移动端自动减少粒子、blur、parallax；
- 避免每帧触发 React state 重渲染；
- 动画尽量只动 transform / opacity；
- 不允许滚动明显掉帧或输入延迟。

---

## 12. 可访问性与降级

必须支持：
- `prefers-reduced-motion`：关闭 pinned cinematic、复杂 parallax、持续花瓣；内容直接正常显示；
- 键盘导航；
- 明确 focus 状态；
- 图片 alt；
- 按钮与链接语义正确；
- Lightbox 可 Esc 关闭；
- 导航菜单有 aria 状态；
- 文本与背景对比足够。

不能因为追求视觉效果牺牲可读性。

---

## 13. 内容保护规则

这是个人作品集，不得为了“看起来像设计稿”而虚构内容。

禁止：
- 虚构客户；
- 虚构奖项；
- 虚构媒体报道；
- 虚构 App 数据；
- 虚构书籍销量；
- 用 Lorem Ipsum 替换已有真实内容；
- 改写用户原有自述的核心含义。

现有文本、项目、作品、履历、图片均优先保留。只有在版式需要时可以精简显示层级，但不能丢内容。

---

## 14. 视觉资产生成标准

如果需要生成新的 Hero 主视觉 / 动态素材，按以下目标生成：

“Dark luxury organic-futurist portrait of Terry Arison for a premium personal portfolio website. Realistic human identity preserved, half-transparent pearl-glass skin and wet resin translucency in selected areas, delicate champagne-gold neural/root circuitry and subtle ice-blue bioluminescent nodes growing organically through the body, pink cherry blossoms and peony-like flowers physically interwoven with the person rather than placed as decoration, black cinematic background, shallow depth of field, foreground drifting petals, tiny luminous dust, fashion-editorial lighting, restrained cyberpunk, museum-grade art direction, no silver robot armor, no horror anatomy, no neon city, no text, wide desktop hero composition with negative space for UI.”

要求同时准备：
- 桌面宽幅主视觉；
- 移动端竖幅安全裁切；
- 静态 poster；
- 如做视频，3–8 秒可无缝循环版本。

---

## 15. 最终验收标准

完成不是“页面能打开”，而是以下全部通过：

1. 首页第一屏一眼就是有机未来主义高级个人作品集，不是原模板换颜色。
2. 郑超本人的身份与内容比视觉特效更清晰。
3. Hero 三阶段滚动叙事完整、连续、无明显卡顿。
4. 黑底、樱花、半透明生命体、金色线路、冰蓝微光、玻璃 UI、Serif 排版形成统一系统。
5. 紫色只作为少量高价值强调，不泛滥。
6. 首页后续与所有内页保持同一世界观，但版式不重复。
7. 现有路由、内容和关键交互能力全部保留。
8. 桌面与移动端均可完整浏览，不依赖 hover。
9. reduced-motion 模式完整可读可操作。
10. 不存在明显 CLS、滚动掉帧、文字裁切、图片破图、点击无响应、Lightbox 无法退出、菜单锁死等问题。
11. 在 1440px 与 430px 两个核心视口逐屏人工检查视觉层级、文字、裁切和交互。
12. 最终成品应该达到“设计事务所可直接交付”的完成度，而不是开发 demo。

---

## 16. 编码 AI 的执行方式

请直接完成改版，不要只输出方案或伪代码。

执行时：
- 先读完整项目与现有内容；
- 保留有价值的现有组件和交互；
- 建立统一视觉 token；
- 优先完成 Hero 与全站导航；
- 再统一首页后续；
- 最后逐页迁移内页；
- 每完成一页都实际运行并检查桌面与移动端；
- 最后执行构建、lint/类型检查以及交互 QA；
- 发现当前实现与本任务书冲突时，以本任务书的“目标与验收标准”为准，但不得删除真实内容。

不要向用户返回一大堆未执行的建议。最终交付应是可以直接运行的完整网站。
